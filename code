#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long a, b;
    
    cin >> a >> b;
    
    if(a % 2 == 1)
    {
        a ++;
    }
    
    if(b % 2  == 1)
    {
        b --;
    }
    
    cout << ((b - a) / 2 + 1) * ((b - a) / 2 + 1);
    
    return 0;
}
